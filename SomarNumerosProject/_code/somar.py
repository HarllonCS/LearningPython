# Iniciar variáveis
n1 = float(input('Digite um número: '))
n2 = float(input('Digite outro número: '))

# Somar
soma = n1+n2

# Exibir mensangem no console
print('A soma entre {} e {} é: {}'.format(n1, n2, soma))
